# Server environment variables
PORT=4000
# When running MongoDB locally (docker-compose maps 27017 to host)
# Use authSource=admin because docker-compose sets root user
MONGODB_URI=mongodb://admin:password@localhost:27017/saas-platform?authSource=admin
JWT_SECRET=your_jwt_secret
NODE_ENV=development
